# Constantine_agent
The chat agent is a single self-contained HTML file — dark, minimal, professional — with your name, title, a typing indicator, suggested opening questions, and full conversation memory within a session. It calls the Claude API directly and speaks in first person as you.
